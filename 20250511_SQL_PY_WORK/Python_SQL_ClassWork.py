# Задание: 
# Создать необходимое количество таблиц для хранения данных в следующей ситуации:
# Человек записывать время, потраченное на различные дела.
# У дела есть название, начало и окончание 
# либо для упрощения: год, месяц, день и час начала и такое же для окончания.
# У дела есть категория (спорт, семья, учёба, работа)
# Программа на питоне выводит общее время, затраченное на все категории,
# и процент потраченного времени. Человек должен спать! Учитывайте это при
# заполнении таблицы.


CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) NOT NULL
);

CREATE TABLE times_times (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    start_time TIMESTAMP NOT NULL, 
    end_time TIMESTAMP NOT NULL,
    category_id INTEGER NOT NULL,
    FOREIGN KEY (category_id) REFERENCES categories(id) 
);

INSERT INTO categories (name) VALUES 
('Спорт'), 
('Семья'), 
('Учёба'), 
('Работа'), 
('Сон');

INSERT INTO times_times (name, start_time, end_time, category_id) VALUES
('Тренировка', '2020-10-01 08:00:00', '2020-10-01 09:00:00', 1),
('Встреча с семьёй', '2020-10-01 10:00:00', '2020-10-01 12:00:00', 2),
('Учёба', '2020-10-01 13:00:00', '2020-10-01 15:00:00', 3),
('Работа', '2020-10-01 16:00:00', '2020-10-01 18:00:00', 4),
('Сон', '2020-10-01 22:00:00', '2020-10-02 06:00:00', 5);